plugins {
    id 'java-app-conventions'
}

group = 'com.empirilytics.qatch.cli'
version = '2.0.0'

dependencies {
    implementation project(":core")
    implementation project(":analyzers")
    implementation project(":calibration")

    implementation 'org.apache.ant:ant:1.10.7'

    implementation 'info.picocli:picocli:4.6.3'
    annotationProcessor 'info.picocli:picocli-codegen:4.6.3'

    testImplementation 'com.github.stefanbirkner:system-lambda:1.2.1'
}

application {
    mainClass = 'com.empirilytics.qatch.cli.Qatch'
    applicationName = 'qatch'
}

javadoc {
    destinationDir = new File(project.rootDir.toString() + "/docs/app/cli/javadoc")
}

