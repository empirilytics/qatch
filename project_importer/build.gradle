plugins {
    id 'java-app-conventions'
}

group 'com.empirilytics.qatch'
version '2.0.0'

repositories {
    mavenCentral()
}

dependencies {
    implementation 'info.picocli:picocli:4.6.3'
    annotationProcessor 'info.picocli:picocli-codegen:4.6.3'
}

application {
    mainClass = 'com.empirilytics.qatch.projectimport.ProjectImporter'
    applicationName = 'importer'
}

javadoc {
    destinationDir = new File(project.rootDir.toString() + "/docs/app/cli/javadoc")
}